# 贡献指南

欢迎帮助改进《Agent Harness：架构、工程与安全》。可以提交 Issue 描述问题，
也可以通过 Pull Request 提交修改。小的文字修订可直接提交；新增章节、研究对象、
比较维度或大范围重写，请先在 Issue 中说明动机和受影响的章节。

## 选择贡献类型

| 类型 | 修改位置 | 建议提供的信息 |
|---|---|---|
| 错别字、术语、表达与导航 | `docs/harness-survey/` 对应章节 | 原句、问题与修改理由 |
| 系统能力或机制纠错 | 机制章、个案章及相关比较表 | 固定提交、源码入口、条件与证据 |
| 学术引用 | 章节与 `references.bib` | 经核对的题名、作者、年份和论文来源 |
| Mermaid 图与表格 | 章节内的图源与表格 | 需要澄清的关系、图表编号与 PDF 页面 |
| PDF 构建与排版 | `docs/book/` | 环境、复现命令、日志与修改前后效果 |
| 网页、导航与搜索 | `docs/web/`、Pages workflow | 页面 URL、视口、主题、搜索词与浏览器结果 |

Issue 中请注明章节文件、标题或图表编号。排版问题附 PDF 页码；源码事实问题注明
版本和前提，避免只说“最新版已经支持”。

## 贡献许可

向本仓库提交原创书稿文字、原创表格或原创图，即表示你有权提交这些内容，并同意将其
按照 [CC BY 4.0](LICENSE) 授权。请不要提交无法按该许可证再分发的第三方内容；引用、
截图、商标和上游材料应清楚标注来源及其适用权利。软件、构建脚本和第三方子模块不在
本书稿 CC BY 4.0 的许可范围内。

## 准备工作区

1. Fork 仓库并 clone 你的 Fork。
2. 从准备贡献的基线创建分支，例如 `git switch -c docs/clarify-session-resume`。
3. 阅读[全书目录](docs/harness-survey/00_index.md)和目标章节；跨章术语以
   [统一参考架构](docs/harness-survey/04_reference_architecture.md)为参照。
4. 需要编译时，按 [README](README.md#生成网页版) 或
   [PDF 构建说明](README.md#编译-pdf)安装依赖。

仅修订文字或编译 PDF，无需下载七个源码子模块。核验某个系统实现时，可以只初始化
对应子模块，例如：

```bash
git submodule update --init --depth 1 codex
```

需要全部源码时执行：

```bash
git submodule update --init --depth 1
git submodule status
```

这些命令检出父仓库记录的提交。随后对照[版本清单](docs/harness-survey/91_version_manifest.md)
核对实际 `HEAD`；如发现差异，先在 Issue 或 PR 中说明。不要使用
`git submodule update --remote` 静默推进研究快照，也不要把上游源码修改混入书稿 PR。

## 内容与证据

本书以固定版本的源码与可核验文献为依据。对实现结论的修改，应在 PR 描述中列出：

- 系统名称、完整 commit，以及源码文件、符号或测试入口；
- 相关配置、功能开关、平台或构建条件；
- 从入口到关键行为的证据，以及本次实际运行过的验证；
- 尚未覆盖的路径、推断或不确定性。

请区分“文档声明”“源码已实现”“默认启用”“运行验证通过”和“推断”。
阅读代码或测试只能支持静态证据；没有实际运行时，不要写成已验证。
“没有找到入口”也不足以证明系统不支持某项能力。

正文应向读者解释机制、边界和设计取舍；详细源码定位与调查记录放在 PR 描述或附件中。
贡献不要求私有台账、特定 Agent、插件或内部工作流。使用 AI 辅助时，由贡献者核对
其结论和引用，并说明影响实质内容的生成、改写与验证范围。

上游版本更新应作为明确的版本更新提案，记录前后提交，并同时核对版本清单、相关机制章、
个案章、横向比较和综合结论。请保留原有结论的版本语境。

## Markdown 与图表约定

- 中文优先；关键术语首次出现时给出中文解释并保留必要英文原词。
- 每个章节一个 H1，使用 H2/H3 组织内容；保留现有章节分工，避免重复解释。
- 以完整段落展开问题、机制、例子与取舍；列表和表格用于真正并列的信息。
- 使用相对链接。修改文件名或标题时同步检查目录和所有引用位置。
- 新引用使用 Pandoc 引用语法，如 `[@citation_key]`；对应条目放入
  `docs/harness-survey/references.bib`，并核实书目信息。
- 四类提示框为“学术背景”“设计取舍”“特色机制”“安全提示”，使用普通 Markdown
  引用块加粗标题，例如下方格式。

```markdown
> **设计取舍｜持久化哪些状态**
>
> 解释方案、收益、代价和适用条件。
```

Mermaid 图直接保存在章节中，图注紧随图源。图号采用章节编号与章内序号：

````markdown
```mermaid
flowchart LR
    A[行动提议] --> B[权限检查]
    B --> C[执行与观察]
```

*图 5-1　行动从提议到执行的示意。*
````

示例仅展示格式；修改时应沿用目标章节实际编号，避免重复。长图应在 Markdown 图源中
调整分组和布局，避免只修改生成的图片。表注使用 `*表 X-Y　说明。*`，统一置于表格上方；
源码表格保持章内顺序，构建器会统一编号。正文可用“图 X-Y”“表 X-Y”交叉引用。

构建器目前固定汇编 36 个章节前缀；新增或重编号章节时，需要同步调整目录、相关链接、
`docs/book/book-manifest.json` 和数量检查，并完整验证 PDF 与网页。

详细叙事、比较公平性和证据约定见[编辑规范](docs/harness-survey/WRITING_PLAN.md)。
其中早期章节规划和阶段清单属于历史设计；贡献流程以本指南为准，排版行为以
[构建文档](docs/book/README.md)及现行脚本为准。历史审阅报告也不代表问题目前仍未解决。

## 提交前检查

任何修改都应检查差异范围与空白问题：

```bash
git diff --check
git diff --stat
git status --short
```

检查新增文件时，在暂存预期文件后再运行 `git diff --cached --check` 和
`git diff --cached`，确认提交包含的内容完整且没有无关文件。

- 文字和导航修改：检查术语、相对链接、标题锚点和引用键。
- 修改正式章节、图表、引用或构建资产：执行 `docs/book/build.sh`，检查退出状态、
  日志和受影响 PDF 页面；同时执行 `docs/web/build.sh`，检查受影响网页、目录、链接和
  搜索结果。图表修改还应检查 GitHub 中的 Mermaid 展示。
- 只修改 Web 主题、模板、搜索或 workflow：运行全部 Web 行为测试、完整
  `docs/web/build.sh`、shell/Node 语法检查和 workflow YAML 解析；涉及发布链时还应先生成
  PDF，并以 `HARNESS_WEB_REQUIRE_PDF=true` 验证下载文件与链接。
- 只改根目录说明或历史过程文档：检查 Markdown、链接与示例命令即可。
- 无法完成某项验证时，在 PR 中写明缺失环境或未验证的部分。

编译成功不替代页面检查。请确认中文和粗体正常、图中文字可读、图注位置合理、
表格没有裁切，并检查受改动影响的页眉、页脚与分页。

默认提交可复现的源文件；生成的整本 PDF、`site/`、临时图、字体缓存、构建日志及依赖
目录不随普通 PR 提交。需要更新发布成品时，请在 PR 中明确说明。

## Pull Request 说明

每个 PR 尽量解决一个可清楚描述的问题。可以使用下面的简短结构：

```text
问题：什么内容不准确、不清楚或无法编译？
修改：涉及哪些章节或构建资产，读者会看到什么变化？
证据：源码版本与入口，或核对过的文献来源。
验证：实际执行的命令、结果与检查过的 PDF 页面。
限制：未验证的条件，或需要维护者继续确认的事项。
```

收到反馈后，按讨论范围更新修改与验证结果。大幅改变研究范围或子模块版本时，
请在 PR 中单独说明，方便维护者评估对全书结论的影响。
